import {SvgProps} from 'react-native-svg';

export interface IconProps extends SvgProps {
  size?: number;
  color?: string;
}
export interface IconWidthHeightProps {
  width?: number;
  height?: number;
  color?: string;
}
